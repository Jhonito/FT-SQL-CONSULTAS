SELECT*

FROM MOVANIO
WHERE TRANSAC = 'aj'
--AND CANTIDAD > 0
order by F_MOVIM  desc,CANTIDAD ASC


SELECT
REFERENCIA,
F_MOVIM,
ARTICULO,
DESCRIP,
CANTIDAD,
DESC_MOV,
ANTEXIS,
ULTEXIS

FROM MOVANIO
WHERE TRANSAC = 'aj'
--AND CANTIDAD > 0
order by F_MOVIM  desc --,CANTIDAD ASC